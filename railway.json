{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/bot && npm ci --force || npm install --force && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd apps/bot && npx prisma db push && npm run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
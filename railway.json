{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/bot && bun install && bunx prisma generate && bun run build"
  },
  "deploy": {
    "startCommand": "cd apps/bot && bunx prisma db push && bun run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
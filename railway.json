{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/bot && npm ci && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd apps/bot && npx prisma migrate resolve --applied 00000000000000_init && npx prisma migrate deploy && node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
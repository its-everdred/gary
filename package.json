{
  "name": "gary-bot-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd apps/bot && npm install --omit=dev && npx prisma generate && npm run build",
    "start": "cd apps/bot && npx prisma db push && npm run start"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}